#!/usr/bin/make -f

override_dh_auto_test:
	echo "Skipping tests due to network dependency." 

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_installman:
	dh_installman debian/man/huggingface-cli.1
